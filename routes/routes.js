const express = require('express');
const router = express.Router();
//const userCtrl = require(...../controller/api?)

// POST routes here
// example: router.post('/', usersCtrl.create)
// GET routes here
// example: router.get('/checktoken', ensureLoggedIn, usersCtrl.checktoken)

module.exports = router;
